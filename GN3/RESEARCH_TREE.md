# GN3 Research Tree

This file is the mutable hierarchical model of the live search. It is **not canonical mathematics**, not an archive of discoveries, and not an audit ledger. Researchers normally do not edit it directly. After each numbered guidance wave, the Director or a delegated Vice Director compresses the tagged Slack results into the smallest hierarchy that still reflects the best current mathematical picture.

Nodes are semantic mathematical objectives, obligations, arguments, or competing approaches. They may be renamed, merged, moved, rewritten, or deleted aggressively. Git history is sufficient recovery for discarded branches.

## Two-tight-paths conjecture

### Close the augmentation gap for an extremal spanning three-path cover

**Target.** Let `F` be a spanning three-path cover of a smallest counterexample such that no endpoint can be transferred from one component into another component of at least equal order. Prove that either a spanning two-path cover exists or there is a spanning three-path cover `F'` with `lambda(F')` lexicographically larger than `lambda(F)`.

For the lexicographically maximal cover already chosen in the proof spine, the second outcome is impossible. The missing argument must therefore produce a literal spanning replacement or force the contradiction through an equivalent global mechanism.

#### Build a literal spanning replacement from the available local structure

Current relevant inputs include the fixed-pair continuation results of Section 4, the deletion/crossing mechanism of Section 5, the extremal-cover restrictions of Section 6, and the ordered-path intersection lemmas in Appendix A.

A successful branch must explicitly account for retained subpaths, joins, required tight triples, pairwise disjointness, and every displaced vertex. A local contact statement alone does not close this node.

#### Seek a cleaner parent theorem or global mechanism

A bounded alternative route is welcome if it genuinely bypasses the local augmentation construction, strengthens it to a more natural parent theorem, or reveals an invariant that makes the lexicographic improvement unavoidable.

#### Identify genuine obstructions to the present augmentation formulation

If the target as stated needs an additional hypothesis, has a structural exceptional configuration, or is false in its present strength, isolate that precisely. “No proof found” does not populate this branch by itself.

## Current guidance wave

No guidance wave has yet been recorded under the new `[G##]` protocol.