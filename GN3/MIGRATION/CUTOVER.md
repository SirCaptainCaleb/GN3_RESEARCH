# GN3 cutover record

Date: 2026-09-17

Status: **GN3 is now the authoritative active research project.**

## Fresh-worker startup test

Checklist item 43 passed on a genuinely fresh worker initialized only from the canonical GN3 architecture location.

The worker explicitly confirmed that its first architecture read was one complete internally consistent revision of `GN3/ARCHITECTURE.md`, beginning at `# GN3 Architecture` and continuing through the literal `## End of complete architecture` marker, before using Slack or doing mathematics.

Its initialization report correctly identified:

- the theorem: every finite 3-uniform boundary tournament should have path-cover number at most two;
- the single canonical proof spine `GN3/PROOF_SPINE/TWO_TIGHT_PATHS.md` rather than an Engine dependency graph;
- the audited coordinate through Proposition 6.2 and the still-open augmentation bridge;
- the need for a literal spanning replacement in any augmentation proof;
- the intrinsic, mathematically compilable proof-coding policy and the prohibition on treating deletion/restriction as reachability;
- abstraction-first, increasing-resolution research and truthful null returns;
- exact-text conservative certification;
- A7C3 as archaeology/provenance only;
- the canonical live Slack set `#gn3-changelog`, `#gn3-research`, `#gn3-audit`;
- `#gn3-lab` as transitional legacy rather than a canonical GN3 surface.

The startup test therefore establishes complete and consistent retrieval and comprehension of the permanent architecture.

## Authority cutover

From this record onward:

- `GN3/ARCHITECTURE.md` is the sole durable architecture authority.
- `GN3/PROOF_SPINE/TWO_TIGHT_PATHS.md` is the canonical active proof attempt.
- `GN3/STATUS.md` records certification/open status.
- `GN3/RESEARCH_STATE.md` is the compact ordinary-research startup state.
- `GN3/RESEARCH_PROTOCOL.md` governs research-progress semantics.
- `GN3/TOOLKIT/README.md` indexes the selected reusable theorem shelf.
- Active research belongs on the canonical `gn3-*` Slack surfaces.

The A7C3 repository tree, A7C3 Slack channels, A7C3 Canvases, and the transitional renamed `#gn3-lab` are no longer active architecture or ordinary research surfaces. They remain available for provenance and archaeology when deliberately needed.

No ordinary GN3 startup path depends on either the A7C3 Canvas or the temporary migration Canvas.

## Canvas-independence verification

The permanent architecture contains all rules needed for ordinary initialization and points only to GitHub canonical state plus the three live GN3 Slack surfaces. Migration evidence under `GN3/MIGRATION/` is explicitly archaeology and is not required startup context.

Therefore Slack Canvas dependence has been removed before cutover.

## Archaeology policy

Legacy A7C3 material remains readable when a provenance question, forgotten proof detail, historical counterexample, or verification task actually requires it. It is pulled deliberately, not pushed into ordinary context.

## Reconstruction/compression maintenance rule

Reconstruction and compression remain available as occasional mathematical maintenance tools when conceptual sprawl or a major discovery makes them useful. They are not optimization metrics. GN3 does not target minimum file length, minimum lemma count, minimum Engine count, or maximum compression ratio; the criterion is improved mathematical comprehension and research effectiveness.

## Remaining migration action

Checklist items 43 and 48–51 are complete. The only remaining migration action is item 52: deletion of the temporary migration Canvas, which requires explicit user permission.
