# The fully anchored fixed-endpoint phase-zero sector has no portal-free recurrence

**Workspace:** D17
**State:** established
**Key:** `fully-anchored-phasezero-collapse-modulo-explicit-portals`

**Summary:** Combine the fixed-endpoint completed macro descent, PAYABLE-FOUR cancellation, middle-fan collapse, and stationary-middle selected-incidence compiler. Fix E={a,c} and suppose A_E=E at an E-aligned mass-two checkpoint. Any certificate-bearing pair excursion is a completed paid macro: it either closes, returns with strict old-E descent, or reaches a rank-flat aligned return. At a rank-flat return, choose the R1022 internal middle b and inspect the first current selected incidence. The completed pre-singleton ancestors at a,c give the bilateral replay-breaker. If a P4 appears, it is PAYABLE-FOUR and cancels. If the predecessor-equality atom appears, two distinct middles force a P4, so only one physical stationary middle can remain. At that unique middle, adding the current exact H-E frame and selected predecessor/successor invokes the stationary-middle compiler, yielding PAYABLE-FOUR or explicit nonquiet geometry. Therefore there is no portal-free rank-flat fully anchored recurrence at fixed E. Every completed phase-zero macro from the bottom stratum yields TWO-COVER or explicit nonquiet portal geometry. The remaining global obstruction is exactly absorption of those explicit portals; payment, middle rebasing, predecessor equality, and silent representative replay are no longer independent failure modes.

### 1. Bottom stratum
Fix a physical endpoint pair

  E={a,c}

with completed endpoint ledger

  A_E=E.                                                  (FC.1)

Work at an E-aligned ancestry-bearing both-singleton floor, so

  Psi_E=(0,0,0).                                          (FC.2)

No strict numerical decrease inside the same fixed-E triple is possible below this point. Hence any portal-free continuation would have to recur rank-flatly.

### 2. Paid pair excursions cannot silently recur
By SV57422, every completed certificate-bearing pair excursion at fixed E gives

  TWO-COVER,
  STRICT Psi_E descent,
  or EXPLICIT NONQUIET PORTAL.                            (FC.3)

At the bottom tuple the strict alternative is unavailable. Therefore any nonclosing completed pair excursion must expose explicit nonquiet geometry unless it returns rank-flatly before its final appended incidence episode.

At such a rank-flat return, SV57138/R1022 supplies a current exact H-E frame with some internal middle b and a proper turn on E. The historical endpoint ledger survives unchanged.

### 3. Bilateral pre-singleton ancestry breaks bare replay
Retain the completed pre-singleton endpoint dimers from SV78682. At the new middle b, each side independently gives

  literal P4,
  reverse trimer,
  or adjacent reversal.                                  (FC.4)

If a literal P4 appears, SV78086 makes it PAYABLE-FOUR and hence it falls under (FC.3). If an adjacent reversal appears, that is already an explicit nonquiet portal.

The only bounded replay atom left by the bilateral analysis is predecessor equality: the two old endpoint dimers share one common secondary vertex x and the new middle b satisfies

  (b,a,x), (x,c,b), (a,b,c) tight.                       (FC.5)

### 4. Predecessor equality cannot move through two middles
SV79142 proves that two distinct physical middles b,d satisfying the equality atom force a literal proper P4 by one R3 test. That P4 is PAYABLE-FOUR. Therefore a portal-free rank-flat lineage can use the predecessor-equality atom at at most one physical middle.

Thus all middle-rebasing ambiguity collapses to one stationary physical middle b.

### 5. The stationary middle is consumed by current selected incidence
Uniqueness of b is only historical geometry, not representative uniqueness. SV79258 correctly fences that issue. At the stationary middle one must use the CURRENT exact H-E frame and its selected predecessor/successor of b.

SV79352 performs exactly that calculation. The first current selected-incidence split at b yields

  PAYABLE-FOUR,
  or EXPLICIT NONQUIET PORTAL.                            (FC.6)

PAYABLE-FOUR again falls under (FC.3). Hence the stationary equality atom cannot support a silent current recurrence.

### 6. Fully anchored collapse modulo explicit portals
Combining Sections 2-5, every fixed-E continuation from the fully anchored bottom checkpoint has the following completed-macro outcome:

  TWO-COVER,
  or EXPLICIT NONQUIET PORTAL.                            (FC.7)

There is no third portal-free rank-flat recurrence mechanism. In particular all of the following are eliminated as independent obstructions:

- certificate-bearing pair payment and floor steering;
- middle rebasing;
- completed-endpoint ledger reset;
- predecessor-equality motion through different middles;
- stationary-middle replay without current-frame data;
- literal P4 transport.

The sole remaining phase-zero obstruction alphabet is explicit nonquiet geometry: R436 growth/cycle/reversal/contact, R435 reversal/trimer/cycle, or equivalent bounded portal cells exposed by the compilers.

### 7. Global significance
This is the exact structural endpoint of the G21 fixed-endpoint program. The fixed-endpoint phase-zero state space is well founded above the bottom tuple, and at the bottom tuple it has no portal-free recurrence. Therefore any failure of full global Morse extinction must now pass through an explicit geometric portal.

The next parent theorem should not add another return coordinate. It should absorb the finite explicit portal alphabet back into TWO-COVER or a source-visible PAYABLE-FOUR/pair excursion already covered by the completed macro calculus.

R24 and R5 are unused.