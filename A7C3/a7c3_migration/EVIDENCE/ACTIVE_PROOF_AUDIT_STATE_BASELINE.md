# Active A7C3 proof/audit state at GN3 migration baseline

This file is durable migration evidence. It records the Slack-only material needed to reconstruct the live mathematical frontier at migration start. It is **not** GN3 mathematical exposition and does not impose legacy terminology or proof topology on the reconstructed GN3 proof.

Baseline repository commit: `7b68b016d3fd309f8a81070bd140e4718d309f53`.

Baseline Slack coordinate: changelog `64`, guidance `G53`, architecture `A47`.

## Live route

The shortest active route at the baseline was

`E8997 -> E9007 -> E9008`.

The older first-source descent sequence `E8997 -> E8998 -> E9003` remained valid reusable mathematics. E9003 remained published reusable machinery. E9006 was bypassed on the shortest route and under payment-proof integrity review. E8999/E9000 were retired provenance.

R24 and R5 were quarantined and unusable. O4 remained open; O6 remained dormant.

## E8997 — certified order gate

Slack Engine root at baseline:

> `[PASS] E8997 — Small-order qualification engine.` Input: a hypothetical smallest Strong Level-(1) boundary-tournament counterexample `H`. Output: either an immediate spanning two-cover/contradiction in orders at most ten, or the certified order gate `|H|>10` used by E8998. The proof is standalone and R24/R5-free. Every order `<=10` closes inside the Engine; the sole surviving export is `|H|>10`.

Terminal composition R2152 passed independent whole-document audit without material repair. The proof was published durably at `A7C3/ENGINES/E8997.md`, commit:

`dafe2ac5ae7a19efc62b93f345320d8ee7e3e900`.

Therefore the Slack thread itself is not load-bearing for recovery of the E8997 proof; the published manuscript is.

## E9007 — theorem interface supported, publication proof not yet valid

Slack Engine root at baseline:

> `E9007 — Fixed-pair payment-sensitive strict return.` **Contract frozen for terminal composition; not yet `[PASS]`.** Input: fixed physical pair `E={a,c}` with source-frame/portal ancestry; for the completion theorem it suffices that distinct `s,t` outside `E` have the same source orientation `(a,s,c)` and `(a,t,c)` (or reversed outer orientation). Output: a spanning two-cover, or a complete ancestry-retaining payment macro returning to literal `E` while a named opposite-polarity `2+2` birth strictly clips to its two signed-anchor singleton descendants `1+1`. A genuinely later remint is downstream consumption and belongs to E9008.

### Certified theorem-level completion mechanism

PASS R2222 supplied the frozen theorem-level mechanism. At a fixed `E={a,c}`, among any three vertices outside `E` two share a source orientation. Name two such vertices `s,t` with `(a,s,c)` and `(a,t,c)` tight. These two turns display disjoint opposite-polarity source-boundary dimers `(a,s)` and `(t,c)`, with signed anchors `s,t` and nonanchors `a,c`. The lawful endpoint-selective payment theorem can prescribe the nonanchors `a,c`, returning to the same literal floor `E` outside TWO-COVER. Core S9010 away-from-anchor contact then clips the historical dimers to strict singleton descendants `(s)` and `(t)`.

PASS R2223 gave optional fixed-pair bilateral harvesting for the downstream remint problem; it was not needed to complete the E9007 one-return contract.

### Two invalidated terminal compositions

R2225 was independently invalidated as a composition draft. Its defect was not the frozen E9007 theorem: it attempted to prove endpoint-selective payment by voluntarily deleting the unchosen endpoint of a signed dimer and treating the survivor as a lawful singleton coordinate. Certified R2185 specifically forbids that shortcut as a replacement for the accepted continuation: first-coordinate endpoint selection must be achieved by the R224 continuation and the second coordinate by R433. The draft also used unsupported unsigned-end refund bookkeeping. The audit classified this as a major proof regeneration.

R2228 was then independently invalidated for the same deeper abstraction error. It tried to reconstruct the payment layer from S9014 cut memory. S9014 only remembers a signed interval **after a lawful marker/deletion**; it does not itself create a current descendant balanced pair or paid floor. R2228's singleton/nontrivial refund lemma also contradicted the accepted R175 semantics: either antisymmetric boundary orientation is the refund marker; a tight attachment is not automatically a closing branch.

The second audit therefore preserved R2222's theorem/interface but required another fresh composition and whole-document audit. The recommended terminal proof was deliberately tiny: form the R2222 source-boundary `2+2`, invoke the certified lawful payment interface with prescribed nonanchors `a,c`, then apply S9010 away from the signed anchors. Do not locally re-prove payment from S9014.

### Accepted payment subsystem and promotion candidates

The accepted historical payment continuation is the R175/R427/R428/R224/R433 subsystem. R2185 had already passed and gives endpoint-selective two-dimer steering.

At the baseline two promotion-form candidates existed but had not yet been independently certified as new GN3/A7C3 durable statements:

**R2232 — endpoint-selective two-dimer floor steering.** If an ancestry-bearing balanced-pair lineage contains two physically disjoint opposite-polarity signed dimers `D_1,D_2`, each certified by a named inherited tight turn, then for arbitrary prescribed endpoints `x∈V(D_1)` and `y∈V(D_2)` there is a finite certificate-retaining continuation to TWO-COVER or the exact ancestry-bearing floor `(x)|(y)`.

Its proof is a direct composition of accepted interfaces. Apply historical R224 to `D_1`, choosing endpoint `x`, obtaining TWO-COVER or `(x)|(q)`. If `q=y`, stop. Otherwise apply historical R433 with `(x)` fixed and target `y`, obtaining TWO-COVER or `(x)|(y)`. The original dimer certificates remain historical graph-intrinsic data; no simultaneous-currentness claim is made. This is mathematically the already-PASS R2185 theorem restated in promotion form.

**R2233 — large-reservoir explicit-marker endpoint steering.** In a hypothetical smallest Strong Level-(1) counterexample with `|H|>=6`, for two physically disjoint opposite-polarity signed dimers with inherited certificates and prescribed endpoints `x,y`, the same endpoint-selected continuation is reconstructed using explicit physical refund markers before every S9014 cut. The proof first uses two outside vertices to orient a tight trimer meeting the first dimer exactly at its unwanted endpoint, then invokes S9014 only after that lawful marker. It then uses minimality to obtain an outside path and uses whichever antisymmetric trimer orientation is tight as the literal refund marker for the second dimer; no tight branch is automatically called closing. If the second signed anchor is not the desired endpoint, pair-deletion rigidity plus a fresh signed dimer and another explicit refund marker installs the target endpoint. Every support cut is preceded by a physical marker. In the live O4 range `|H|>10`, the reservoir hypothesis is automatic.

The migration-baseline strategy preferred R2233 as candidate durable payment theorem S9044 **if it later passed independent audit**; otherwise it would fall back to R2232 plus the accepted historical payment subsystem. No PASS record for R2233 had appeared in Slack at the baseline snapshot.

## E9006 integrity warning

The audit of R2228 exposed materially similar S9014-only refund prose in the already-published E9006 manuscript, especially the singleton/nontrivial payment step. Consequently E9006 was under explicit integrity review and was **not** an active payment source. This did not change the shortest topology because E9006 was already bypassed.

The precise fence is important for recovery: S9014 is cut memory after a lawful physical marker/deletion, not a theorem that manufactures an active paid floor.

## E9008 — live remint frontier

Slack Engine root at baseline:

> `E9008 — Spent-anchor remint extinction.` Input: a fixed physical pair `E={a,c}`, retained ancestry from a completed E9007 strict-return macro (strengthened when useful by fixed-pair bilateral source harvesting), and a genuinely later opposite-polarity `2+2` remint before unrelated reset. Output: a spanning two-cover, or a physical interaction/descent that consumes the remint against retained spent history and cannot be discharged by exact-support/certificate replay.

A “genuinely later remint” means a `2+2` pair whose role as current/retained birth is generated by an actual post-E9007 payment/return event or later physical proposal. Re-declaring an old graph-intrinsic signed pair is not a remint. Exact reuse of old physical support with a fresh witness is still replay unless the argument consumes genuinely post-payment physical role/provenance.

### Certified compression at baseline

**PASS R2224.** At fixed `E={a,c}`, pivot reuse within each source-orientation class harvests every class of size at least two completely. In the live range, at most one exterior vertex is cold. Therefore every later physical disjoint-dimer `2+2` remint meets a bilaterally harvested vertex; there is no four-vertex cold remint support left to classify.

**PASS R2226.** Naked singleton incidence is not progress. If a later paid floor contains a harvested source middle `s`, the two S9010 at-anchor contacts with historical `(a,s)` and `(s,c)` may both replay completely through the original source trimer: singleton `(s)` can regrow to `(a,s)` or `(s,c)`. E9008 therefore must consume additional remint-specific data rather than merely contact harvested ancestry.

**PASS R2229.** If a genuinely later nontrivial remint dimer meets harvested source middle `s` and its other endpoint `u` lies outside the old source trimer `{a,s,c}`, Core S9010 forces strict growth or reverse-contact geometry containing the physical vertex `u`. Hence pure source-trimer replay at harvested `s` is localized to exact old source-boundary supports `{a,s}` and `{s,c}`. This is localization, not closure: the `u`-bearing geometry still requires consumption.

**PASS R2231.** Four same-oriented source spokes at a fixed outer pair force a literal SPECIAL P5 `(x,a,y,c,z)` by a hypothesis-free two-tournament argument.

**PASS R2230.** Combining the four-spoke theorem with harvesting, the live `|H|>10` setting admits a SPECIAL P5 whose three spokes `x,y,z` are all bilaterally harvested.

### Exact frontier

The live obstruction at baseline had therefore been compressed to two consumers:

1. consume the new-endpoint-bearing growth/reverse-contact geometry emitted when a remint dimer has genuinely new endpoint `u`; or
2. extinguish the exact old boundary-support replay residue `{a,s}` / `{s,c}` by coupling the incident remint dimer to the **other remint dimer** and crossed historical/SPECIAL-P5 data before singleton collapse.

Another payable birth, singleton contact, fresh witness name, off-`E` floor, or bare nonquietness is not terminal progress.

Existing remint-specific auxiliary consumers included source-class four-set closure R2218, swap-square diagonal P4/collision geometry R2213-R2216, and batch historical-support clipping R2219.

## Recovery summary

The key live mathematical facts to carry through migration are therefore:

- certified order gate `|H|>10` (published E8997);
- E9007 theorem-level one-return interface supported by R2222, but **no valid terminal publication manuscript yet** at baseline;
- lawful endpoint-selective payment must come from accepted payment machinery or a later independently audited replacement, never bare S9014 truncation;
- E9006 payment proof has an integrity flag and is not an active payment dependency;
- E9008 is the genuine live problem, localized to remint interaction with spent history, especially exact source-boundary support replay.

These facts are migration evidence only. The supervised GN3 blank-page reconstruction is free to merge, inline, split, or discard all legacy Engine/result boundaries.
