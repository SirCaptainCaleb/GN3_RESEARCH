# R691 — Same-polarity inward anchor exchange captures the inward Q vertex itself

**Record status:** `USABLE/LEGACY`

## Statement
Retain the accepted crossed R621/R649 frame with X={a,b,c,z}, Q=(q_0,...,q_m), m>=6, L=q_0 and R=q_m. Let S be any tested orientation of one crossed middle-gate dimer G_3={a,c} or G_4={b,z}. Suppose an anchor-exchange output supplies one of the two inward Q vertices v in {q_2,q_{m-2}} as a new same-polarity witness on S, so that the corresponding three-vertex carrier K on V(S) union {v} is tight: for G_3 this means the head-sign trimer K=(v,S), while for G_4 it means the tail-sign trimer K=(S,v), with the exact tested order retained. Then v itself has a valid historical capture certificate through a literal old-Q selected state in an exact H-V(S) two-cover. More precisely, if v=q_2, choose an R621-punctured exact H-V(S) return containing Q[1,m] or Q[2,m] as appropriate; the physical state D={q_2,q_3} is selected and is a carrier/exterior crossing for K. If v=q_{m-2}, choose the dual exact return containing Q[0,m-2] or Q[0,m-1]; D={q_{m-3},q_{m-2}} is selected and is a carrier/exterior crossing for K. R649 supplies a same-polarity endpoint witness outside K that avoids D (take R for v=q_2 and L for v=q_{m-2}). Hence accepted R527 applies and, by orienting the marker appropriately, designates v itself as the captured signed anchor. Since v has the genuine historical carrier adjacency to its neighboring S-vertex inside K, accepted R312 yields a chosen protected continuation on which either an earlier event touching v is exposed or the lawful old-carrier mirror reentry through that X-Q adjacency is itself first contact at v. Thus every same-polarity middle-gate R542 branch created by an inward anchor exchange already forces a protected contact at the strict interior Q coordinate q_2 or q_{m-2}; it need not be spent as anonymous X-dimer payment.

## Hypotheses
H is a hypothetical smallest Strong Level-(1) counterexample in the accepted crossed R621 frame; m>=6. S is one tested orientation of G_3={a,c} or G_4={b,z}. The input branch supplies q_2 or q_{m-2} as a same-polarity witness on S, hence a literal tight trimer K on S plus that inward anchor. Use accepted R621 exact singleton-deletion weaves, accepted R649 fixed endpoint witnesses, R527 same-frame capture, and R312 protected old-carrier reentry.

## Scope
Noncircular R24 consumer of same-polarity X-dimer anchor-exchange branches in the accepted sigma=0 A-F portfolio. It creates strict physical Q-interior capture/contact without R5,R168,R169,R224,R533,generic R358,R436, or generic pair payment.

## Applicability
Whenever R677-R681 or another crossed anchor-exchange certificate yields q_2 or q_{m-2} as a same-polarity witness on an oriented G_3/G_4 dimer, stop treating the output as merely R542-ready. Use the displayed R621 punctured exact return, capture the inward Q vertex itself by R527, then run R312 on the new X-Q carrier adjacency. Preserve the literal selected old-Q crossing and the tested orientation of S.

## Persistence
Retain S and its tested order, inward anchor v, tight carrier K=S union {v} with exact order, fixed R649 witness w, the chosen punctured exact H-V(S) cover, literal selected crossing D={q_2,q_3} or {q_{m-3},q_{m-2}}, marker orientation designating v, historical capture of v, genuine old-carrier X-Q adjacency E inside K, and the R312 protected-contact lineage.

## Exclusions / nonclaims
Does not consume opposite-polarity anchor-exchange branches, which are labelled Hamilton P4s. Does not consume same-polarity packets whose new witness is not q_2 or q_{m-2}. Does not say first contact alone closes H. The R312 earlier-contact alternative need not itself use the X-Q carrier adjacency; the mirror-reentry alternative does. Does not assert the exact deletion cover remains current after capture. Does not identify generic R542 payment with progress; the gain is the captured strict interior Q anchor and retained physical crossing.

## Record notes
- **Lifecycle:** retired
- **Lifecycle note:** Same-polarity inward-anchor direct-capture leaf with no consumers; current work keeps physical seam/cut information rather than generic capture.
- **Review status:** accepted
- **Review note:** The four punctured R621 pair-deletion returns are literal and exact; the selected old-Q state is a valid carrier/exterior crossing, R649 supplies an avoiding same-polarity endpoint witness, R527 may capture the inward anchor itself, and R312 uses a genuine old-carrier X-Q adjacency.

## Proof
Write d=q_{m-2} and r=q_{m-3}. We treat four source combinations; tested orientation inside each middle gate is arbitrary and is carried literally by the input trimer.

LEFT ANCHOR v=q_2, S=G_4={b,z}. R621 gives the deep exact singleton-deletion covers H-b=(q_1,L,c,a,z) sqcup Q[2,m] and H-z=(q_1,L,a,c,b) sqcup Q[2,m]. Delete the other vertex of S from the displayed short rail: deleting z from the first gives (q_1,L,c,a) sqcup Q[2,m], while deleting b from the second gives (q_1,L,a,c) sqcup Q[2,m]. Either is a literal two-cover of H-{b,z}; it is exact, because if H-{b,z} were Hamiltonian then its Hamilton path together with the tight dimer on {b,z} would two-cover H. Choose either return. The input same-polarity G_4 branch is a tail certificate, so in its exact tested order K=(S,q_2) is a tight trimer on V(S) union {q_2}. In the chosen exact H-S cover the long rail begins q_2,q_3,..., hence D={q_2,q_3} is a selected state. Relative to K, C=V(K)\V(S)={q_2}, while q_3 lies outside K, so D is a carrier/exterior crossing. Accepted R649 tail-signs either tested orientation of G_4 by the physical endpoint R=q_m. Since m>=6, R is outside K and R notin D. Apply R527 with witness R and crossing D, orienting the two-vertex marker so that q_2 is the designated captured signed anchor. This gives a valid historical capture certificate for q_2. The retained historical carrier K has the genuine adjacency E between q_2 and the adjacent S-vertex in K. R312 therefore gives a protected continuation with earlier-contact-at-q_2 or mirror-reentry-first-contact-at-q_2 through E.

LEFT ANCHOR v=q_2, S=G_3={a,c}. Use the R621 shallow covers H-c=(a,b,z,L) sqcup Q[1,m] and H-a=(c,z,b,L) sqcup Q[1,m]. Deleting a from the first gives (b,z,L) sqcup Q[1,m], and deleting c from the second gives (z,b,L) sqcup Q[1,m], literal two-covers of H-{a,c}; exactness follows from the same Hamilton-residue-plus-deleted-dimer contradiction. The input same-polarity G_3 branch is a head certificate, so K=(q_2,S) is tight. The common long rail Q[1,m] contains the selected state D={q_2,q_3}. Again C={q_2}, q_3 is exterior to K, and R is an R649 same-head witness on either tested orientation of G_3 avoiding D. R527 captures q_2, and R312 applies to the genuine K-adjacency between q_2 and S.

RIGHT ANCHOR v=d=q_{m-2}, S=G_3={a,c}. Use the R621 deep covers H-a=Q[0,d] sqcup (c,z,b,R,q_{m-1}) and H-c=Q[0,d] sqcup (a,b,z,R,q_{m-1}). Deleting c from the first gives Q[0,d] sqcup (z,b,R,q_{m-1}); deleting a from the second gives Q[0,d] sqcup (b,z,R,q_{m-1}). These are exact covers of H-{a,c}. The same-polarity G_3 input is a head trimer K=(d,S). The long rail contains selected D={r,d}. Take L as the fixed same-head R649 witness; L lies outside K and avoids D. R527 captures d itself, then R312 probes first contact through the genuine d-S carrier adjacency.

RIGHT ANCHOR v=d=q_{m-2}, S=G_4={b,z}. Use the R621 shallow covers H-z=Q[0,m-1] sqcup (R,a,c,b) and H-b=Q[0,m-1] sqcup (R,c,a,z). Deleting b from the first gives Q[0,m-1] sqcup (R,a,c); deleting z from the second gives Q[0,m-1] sqcup (R,c,a), exact covers of H-{b,z}. The same-polarity G_4 input is a tail trimer K=(S,d). The Q rail contains D={r,d}. Take L as the fixed same-tail R649 witness. R527 captures d and R312 gives the protected contact alternative through the genuine d-S carrier adjacency.

All four cases use a selected old-Q state already present in a literal exact deletion return, but the captured anchor is the strict inward vertex q_2 or q_{m-2}, and the historical carrier adjacency used by R312 joins that inward Q vertex to the middle-gate X dimer. This is a physical support change rather than anonymous certificate reminting.
